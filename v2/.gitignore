# ── 절대 커밋하면 안 되는 것 ──────────────────────────────
# 이 저장소는 GitHub Pages로 공개되므로, 여기 걸리지 않은 파일은
# 인터넷에 그대로 노출된다고 생각하고 다뤄야 합니다.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# ── 빌드 산출물 ───────────────────────────────────────────
node_modules/
.next/
.open-next/
.wrangler/
next-env.d.ts
cloudflare-env.d.ts
*.tsbuildinfo
